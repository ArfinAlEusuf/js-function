function tenTimes (number){
    const reusult = number *10;
    return reusult;
}

function cutHalf(number){
    const half = number / 2;
    return half;
}

tenTimes(5);

const output = tenTimes(5);
console.log('output', output);

const bigNumber = tenTimes(99);